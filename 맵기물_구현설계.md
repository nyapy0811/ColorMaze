# 맵 기물 7종 구현 설계

`ColorMaze 기획서.md` 4장 기준. 이미 완성된 4.2 컬러 필터(`ColorFilterBlock`)를 포함해 7개 기물을 하나의 일관된 구조로 정리한다. 실제 코드 작성 전 설계 단계.

추후 기물이 추가될 것을 감안해, "어떤 효과인가"와 "언제·몇 번 발동하는가"를 분리하는 것을 설계 원칙으로 삼는다.

## 1. 클래스 계층

```
MapObjectBase (abstract, MonoBehaviour, RequireComponent(Collider))
 ├─ FilterBlockBase          판정형(필터) — 4.2, 4.3
 │   ├─ ColorFilterBlock         (컬러 필터)
 │   └─ RgbFilterBlock         (RGB 필터)
 │
 ├─ ClearObjectBase        클리어형 — 4.7
 │   └─ ColorCanvas            (캔버스)
 │
 └─ ConsumableObjectBase   소모성·1회성(범용) — 향후 스택과 무관한 1회성 기물도 여기서 파생
     └─ StackModifierConsumable   스택을 다루는 소모성 기물 전용 중간 계층 — 4.1, 4.4, 4.5, 4.6
         ├─ ColorPalette              (컬러 팔레트)
         ├─ PreviewingStackModifier   발동 전/후 색 미리보기가 있는 기물 전용 중간 계층 — 4.4, 4.5
         │   ├─ StackChanger              (스택 체인저)
         │   └─ ColorChanger              (컬러 체인저)
         └─ Bucket                    (버킷, 색 순환 애니메이션 — 미리보기 없음)
```

`PreviewingStackModifier`는 "발동 전/후 색을 미리 보여주는가"만 다시 분리한 것이다 — 스택 체인저·컬러 체인저는 `currentColorRenderers`/`resultColorRenderers`로 미리보기를 갱신하는 로직(ColorStackChanged·SceneLoadCompleted 구독 + Start 초기 1회)이 완전히 동일해서 이 중간 클래스로 뽑았고, 컬러 팔레트·버킷은 미리보기가 없어 `StackModifierConsumable`을 바로 상속한다.

클릭으로 상호작용하는 기물(캔버스 제외)은 전부 1회용이라, 별도 "획득형(AcquireObjectBase)" 그룹을 두지 않고 컬러 팔레트도 `StackModifierConsumable`을 상속한다 — 스택을 조작하고 조건에 따라 소모된다는 점에서 스택 체인저·컬러 체인저·버킷과 본질적으로 같은 부류이기 때문이다.

`ConsumableObjectBase`를 "스택 변형"과 분리해 둔 이유: 나중에 스택과 무관한 1회성 기물(예: 1회용 발판, 순간이동 패드)이 추가돼도 `ConsumableObjectBase`만 상속하면 되고, `ColorStacks`를 몰라도 된다. 현재 4개 기물처럼 "스택을 조작하는 소모성 기물"에만 `StackModifierConsumable`이라는 한 단계 더 구체적인 중간 클래스를 둔다.

## 2. MapObjectBase — 공통 루트

모든 기물이 공유하는 것만 담는다.

- `[RequireComponent(typeof(Collider))]`
- `protected Collider Col { get; }` — Awake에서 캐싱
- `protected ColorStacks Player { get; } => ColorStacks.Instance;` — 씬에 하나뿐인 플레이어를 `ColorStacks.Instance`(Awake에서 등록되는 정적 참조)로 가져온다(별도 Player 태그 컴포넌트 없이 `ColorStacks` 보유 오브젝트를 플레이어로 간주하는 기존 관례는 유지)
- `protected bool IsPlayer(Collider other) => Player != null && other.GetComponentInParent<ColorStacks>() == Player;` — 4개 그룹 모두에서 반복되는 "이 콜라이더가 플레이어인가" 판정을 한 곳으로 모음

이 공통화 덕분에 각 그룹 베이스는 "무엇을 할지"만 신경 쓰면 된다.

## 3. 그룹별 베이스 클래스

### FilterBlockBase (판정형 — 4.2, 4.3)
- `abstract bool Matches(ColorStacks player);`
- 매 프레임 폴링하지 않고, `ColorStackChanged`(플레이어 스택 변경 시)·`SceneLoadCompleted`(스테이지 시작 시) 이벤트를 구독해 그때만 `Matches(Player)`를 재계산, `Col.isTrigger` 토글과 채움 메시 투명도(`Refresh()`)를 갱신
- `OnTriggerEnter/Exit`: `IsPlayer(other)`로 진입·완전 통과 추적, 통과 완료 시 `Player.ResetAll()`
- 외형(공용 머티리얼·투명도 적용, 메시 병합 연동)은 `FilterBlockBase`에 내장(별도 매니저 컴포넌트 없음). 테두리는 전용 셰이더(`Custom/FilterBorderAlwaysVisible`)로 렌더 큐만 채움보다 뒤로 둬 자기 채움 메시에 가려지지 않게 함
- 하위 클래스는 `Matches()`만 구현:
  - `ColorFilterBlock.Matches` = 정확한 RGB 일치
  - `RgbFilterBlock.Matches` = `Player.GetMaxColors()`에 지정 색이 포함되는지

### ClearObjectBase (클리어형 — 4.7)
- `public bool Completed { get; private set; }`
- `abstract bool CheckCondition(ColorStacks player);`
- `abstract void OnCompleted();` — 완료 시 외형 변경 등
- `OnTriggerEnter`: `!Completed && IsPlayer(other) && CheckCondition(Player)`면 `Completed = true` 후 `OnCompleted()` 호출 + `EventBus.Publish(new CanvasCompleted{...})`
- 한 번 완료되면 계속 유지(잠금) — 순차 완료 방식(이전 설계에서 확정)
- `ColorCanvas.CheckCondition` = 세 스택 값이 목표값과 정확히 일치하는지

### ConsumableObjectBase (소모성·1회성 — 범용)
- `bool Consumed { get; private set; }`
- `abstract void Apply();` — 어떤 자원(스택 등)을 다루는지 모름, 그냥 "발동"만 정의
- `protected virtual bool ShouldConsume() => true;` — `Apply()` 이후 실제로 사라져야 하는지(기본은 항상 사라짐)
- `OnTriggerEnter`: `Consumed`면 무시(중복 발동 방지), `IsPlayer(other)`면 `Apply()` 호출 후 `ShouldConsume()`이 참일 때만 `Consume()`
- `protected virtual void Consume()`: `Consumed = true`, `Col.enabled = false`, `Destroy(gameObject)` (씬에 직접 배치되는 오브젝트라 풀링 대상 아님 — 필요해지면 `PoolManager.Despawn`으로 교체 가능)

#### StackModifierConsumable : ConsumableObjectBase (4.1, 4.4~4.6 중간 계층)
- `abstract void ApplyToStacks(ColorStacks player);`
- `Apply() override`: `ApplyToStacks(Player)` 실행 전후로 R/G/B 값을 비교해 `changedStack` 저장
- `ShouldConsume() override => changedStack;` — 실제로 스택이 하나라도 바뀐 경우에만 소모됨(바뀐 게 없으면 그대로 남음)
- 하위 클래스:
  - `ColorPalette.ApplyToStacks`: 지정된 R/G/B만큼 `player.Add(color, amount)` (각 채널 값이 0이 아닐 때만 호출)
  - `StackChanger.ApplyToStacks`(`PreviewingStackModifier` 경유): `a=Get(colorA); b=Get(colorB); SetValue(colorA,b); SetValue(colorB,a);`
  - `ColorChanger.ApplyToStacks`(`PreviewingStackModifier` 경유): `max=세 스택 중 최댓값; 각 색 c에 대해 SetValue(c, max-Get(c));`
  - `Bucket.ApplyToStacks`: `SetValue(targetColor, 0);`

#### PreviewingStackModifier : StackModifierConsumable (4.4, 4.5 전용 중간 계층)
- `abstract void RefreshPreview();`
- `OnEnable/OnDisable`: `ColorStackChanged`·`SceneLoadCompleted` 구독/해제, `Start()`에서 `RefreshPreview()` 초기 1회 호출 — 스택 체인저·컬러 체인저에 복붙돼 있던 미리보기 갱신 보일러플레이트를 통합한 것

## 4. ColorStacks에 추가할 API

- `SetValue(LightColor c, int value)` — 절대값 지정(내부적으로 `Wrap` 적용) 후 변경 시 이벤트 발행. `Add`도 내부적으로 이를 재사용하도록 정리 가능.
- `GetMaxColors()` — 세 스택 중 최댓값을 가진 색상 목록(동률 포함) 반환. RGB 필터 판정에 사용.

## 5. 새 이벤트

- `CanvasCompleted : IEvent` — 캔버스 하나가 조건을 만족했을 때
- `StageCleared : IEvent` — 스테이지 내 모든 `ColorCanvas`가 완료됐을 때, `LevelManager`가 구독해 클리어 처리

## 6. 구현 순서

1. `ColorStacks.SetValue`, `GetMaxColors` 추가
2. `MapObjectBase` 작성(공통 Player/IsPlayer 로직)
3. `FilterBlockBase` 추출 → 기존 `ColorFilterBlock` 이관 → `RgbFilterBlock` 구현
4. `ConsumableObjectBase` → `StackModifierConsumable` → `ColorPalette`/`StackChanger`/`ColorChanger`/`Bucket` 구현
5. `ClearObjectBase` → `ColorCanvas` 구현 + `CanvasCompleted`/`StageCleared` 이벤트 + `LevelManager` 연동

## 7. 향후 기물 추가 시 참고

- 판정형(통과 조건)만 다른 기물 → `FilterBlockBase` 상속, `Matches()`만 구현
- 클리어 조건만 다른 기물 → `ClearObjectBase` 상속, `CheckCondition()`만 구현
- 스택과 무관한 1회성 기물(예: 1회용 발판) → `ConsumableObjectBase` 직접 상속, `Apply()`만 구현
- 스택을 다루는 새 소모성 기물(획득형 포함, 클릭 한 번에 스택을 조작하고 조건부로 사라지는 기물) → `StackModifierConsumable` 상속, `ApplyToStacks()`만 구현
